sourceCompatibility = 1.8
targetCompatibility = 1.8


mainClassName = 'com.mananger.ads.CampaignsApi'

dependencies {
}

dockerCompose {
    useComposeFiles = ['../docker-compose.yml']
    startedServices = ['postgres']
}
